# VAJANS Frontend — production build env
# Copy to .env.production OR set as a Render env var on the static site.
# Vite reads VITE_* at build time only.

# Backend public URL + /api  (apiClient prepends this to every request path)
VITE_API_BASE_URL=https://vajans-backend.onrender.com/api
